services:
  Ubuntu:
    container_name: Ubuntu_Container
    image: ubuntu:latest
    command: tail -f /dev/null
  nginx:
    container_name: nginx_container
    image: nginx:latest
    ports:
      - "8080:80"
